# Ruby / Rails (Easy8/Redmine plugin) — adjust once actual source is added
*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
/log/
.bundle/
vendor/bundle

# Secrets
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db

# Local reference checkout of the Easy8 app (~260MB) — not part of this repo.
# The engine is overlaid into it by symlink; see CORE_CHANGES.md.
easy8_source_code/
